package generators;

public class SimpleRandomGenerator implements PassGenerator {

    @Override
    public String generate(int length, boolean useDigits, boolean useSpecials) {
        return null;
    }
}
